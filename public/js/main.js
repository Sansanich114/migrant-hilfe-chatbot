// main.js
// Initialization and global event listeners

document.addEventListener('DOMContentLoaded', () => {
    console.log('Main JS loaded');
    // Initialize global event listeners or app state here
  });
  